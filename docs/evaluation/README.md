# Local generated evaluation artifacts

These are actual locally generated outputs and timing reports from the public transcript corpus. They are evidence for format/functionality checks, not automatically validated expert advice. Inspect each cited excerpt before treating a recommendation as fact. Generated model text can be wrong despite a valid citation number.

Failed outputs are recorded in the sanitized development log and verification document. The final report is updated only by the actual evaluation script.
